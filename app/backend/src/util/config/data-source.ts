import { DataSource } from 'typeorm';
import { DotConfig } from './Config';
import 'reflect-metadata';

const AppDataSource = (env: DotConfig): any => {
  // new DataSource({});
  return null
}

export default AppDataSource;
